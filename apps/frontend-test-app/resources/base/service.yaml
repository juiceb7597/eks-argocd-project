apiVersion: v1
kind: Service
metadata:
  name: frontend-test-app-service
spec:
  type: ClusterIP
  selector:
    app: frontend-test-app
  ports:
  - name: http
    protocol: TCP
    port: 80                                      
    targetPort: 10000 